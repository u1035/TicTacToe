<UserControl x:Class="TicTacToe.CellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ticTacToe="clr-namespace:TicTacToe"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance ticTacToe:CellViewModel}">
    <Grid>
        <Button Command="{Binding ClickCommand}" Margin="4" MinHeight="50" MinWidth="50" Content="{Binding Text}" FontWeight="Bold" FontSize="40" BorderThickness="{Binding BorderThickness}" BorderBrush="{Binding BorderBrush}" Foreground="{Binding ForegroundBrush}"></Button>
    </Grid>
</UserControl>
